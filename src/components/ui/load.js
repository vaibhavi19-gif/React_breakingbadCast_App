import React from 'react'
import load from '../../img/load.gif'

const Load = () => {
    return ( <
        img src = { load }
        style = {
            { width: '200px', margin: 'auto', display: 'block' }
        }
        alt = 'Loading...' / >

    )
}

export default Load